# Product

## Register

product

## Users

数据工程师、数据分析师与 DBA,他们在 DeepSeek Harness Web GUI 中创建「数据Agent」预设会话,通过对话让 AI 连数据库、写 SQL、做探查与数据分析。他们通常是重度键盘用户,一天之内多次切换会话与数据库,关心执行结果多于界面装饰。

## Product Purpose

dsh-data-agent 是一个 DSH 插件:为数据Agent 会话提供数据库工作台(连接配置、库表浏览、SQL 命令框),并提供一个只含 sqlcmd/read/write/edit 四个工具的专用 agent 预设,让 AI 与真实数据库形成 Agent Loop——写 SQL、执行、根据报错与结果动态调优。成功的样子是:用户配置好连接后,几乎不再碰工作台,把探索与查询交给 Chat 里的数据Agent。

## Brand Personality

专业、克制、高效。语气平静直接,不卖萌、不夸张;密度适中,信息优先;强调色只在必要处出现(主操作、选中态、状态指示)。

## Anti-references

- 不要做成花哨的「数据大屏」:渐变背景、发光图表、玻璃拟态、大数字指标模板。
- 不要堆装饰性卡片:每个区块都必须对应一个真实操作,而不是填充版面。
- 不要与宿主 GUI 观感割裂:工作台必须跟随 DSH 的浅色/深色主题与设计令牌,不能自带一套硬编码暗色。
- 不要滥用 Modal:库表浏览用 Modal 是合理的(内容量大、需要全神贯注),但内部结构要像文件浏览器一样清晰。

## Design Principles

1. **融入宿主,而非自成一套**:所有颜色、圆角、阴影、字体都取自 DSH 的 `--dsw-alias-*` 设计令牌,主题自动适配。
2. **操作可见,状态自明**:连接、浏览、运行 SQL 是三条主线;每个状态(未连接/连接中/已连接/运行中/出错)都有一眼可辨的视觉表达。
3. **密度服务于效率**:数据工程师习惯紧凑界面,树形列表与表单都保持高信息密度,同时留足点击热区。
4. **等宽字体属于数据**:库名、表名、SQL、执行结果用等宽字体,与普通 UI 文案形成清晰分野。
5. **Modal 内像文件浏览器**:库=目录,表=文件,缩进、图标、连接线一应俱全,让上千张表的库也能轻松导航。

## Accessibility & Inclusion

- 全部交互元素支持键盘操作(Escape 关闭 Modal、按钮原生聚焦)。
- 对比度遵循宿主令牌体系,浅/深两套主题下均可读。
- 状态不只靠颜色表达:连接状态同时有文字标签,错误有图标与文字双重提示。
- 遵循宿主对 reduced motion 的既有处理,不引入装饰性动画。
